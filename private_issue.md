

## Security Analysis: High and Critical CVEs in dotCMS

**OWASP Top 10 Distribution Summary:**
- A03:2021 – Injection: 4 vulnerabilities
- A05:2021 – Security Misconfiguration: 15 vulnerabilities
- A06:2021 – Vulnerable and Outdated Components: 10 vulnerabilities
- A09:2021 – Security Logging and Monitoring Failures: 1 vulnerability

| No. | CVE name/Rule | OWASP Top 10 | Description | Validity | Likelihood | EPSS | CISA KEV | Impact on dotCMS |
|-----|---------------|--------------|-------------|----------|------------|------|----------|------------------|
| 1 | CVE-2025-66516 | A06:2021 – Vulnerable and Outdated Components | Critical XXE vulnerability in Apache Tika tika-core, tika-pdf-module, and tika-parsers allowing XML External Entity injection via crafted XFA files in PDFs. Expands scope of CVE-2025-54988 to include tika-core and 1.x releases. | True Positive 👀 | High | 0.00043 | No | dotCMS uses Apache Tika for content parsing and document indexing. The vulnerable tika-parser-pdf-module is included in dotCMS dependencies for parsing PDF documents uploaded by users. An attacker could upload a malicious PDF containing a crafted XFA form to read sensitive configuration files (e.g., dotmarketing-config.properties), access internal resources, or trigger SSRF attacks against internal services. This could expose database credentials, API keys, and other sensitive configuration data stored in the dotCMS environment. The vulnerability is particularly dangerous in content management scenarios where users can upload documents. |
| 2 | CVE-2025-54988 | A06:2021 – Vulnerable and Outdated Components | Critical XXE vulnerability in Apache Tika tika-parser-pdf-module (versions 1.13-3.2.1) allowing XXE injection via crafted XFA files in PDFs, potentially leading to sensitive data exposure or SSRF attacks. | True Positive 👀 | High | 0.00043 | No | This is the original report of the XXE vulnerability affecting dotCMS's PDF parsing capabilities. The impact is identical to CVE-2025-66516. DotCMS uses Tika extensively in the ContentletIndexAPI and FileAssetAPI classes for indexing and processing user-uploaded content. The vulnerability exists in the document processing pipeline where PDFs are parsed for metadata extraction and full-text search indexing. Attackers could exploit this during content creation, import operations, or when the system reindexes existing content. The risk is amplified in multi-tenant environments where malicious users could access data from other tenants. |
| 3 | java.spring.security.java-sql-sqli.java-sql-sqli | A03:2021 – Injection | SQL injection vulnerability in DotConnect.java at line 724 where untrusted input may be concatenated into SQL queries without proper sanitization, allowing attackers to execute arbitrary SQL commands. | True Positive 👀 | Critical | N/A | No | This is a critical vulnerability in dotCMS's core database abstraction layer. The DotConnect class at line 724 (in the setSQL method) allows dynamic SQL construction. Examining the code, DotConnect is used throughout dotCMS for database operations in critical components like ContentletAPI, StructureAPI, UserAPI, and PermissionAPI. An attacker exploiting this could bypass authentication, access or modify any data in the database (including user credentials, content, and system configurations), execute stored procedures, or potentially achieve OS command execution depending on database permissions. The vulnerability is particularly severe because DotConnect is a foundational class used across the entire codebase, making the attack surface extensive. Exploitation could occur through any user input that flows into SQL queries, including search parameters, filter criteria, or API endpoints. |
| 4 | java.lang.security.audit.formatted-sql-string-deepsemgrep.formatted-sql-string-deepsemgrep | A03:2021 – Injection | SQL injection vulnerability in DotConnect.java at line 682 where formatted SQL strings with untrusted input can lead to SQL injection attacks through string concatenation or formatting. | True Positive 👀 | Critical | N/A | No | This vulnerability exists in the same DotConnect class but at a different location (line 682). This appears to be in a method that handles parameterized SQL but may still allow string formatting vulnerabilities. The impact is similar to finding #3, but this specific instance may be in a different code path. DotConnect's loadObjectResults and loadResults methods are frequently called throughout the application for data retrieval operations. An attacker could exploit this through crafted input in content queries, user management operations, or workflow actions. Given that dotCMS supports custom fields and dynamic queries for content types, there are numerous potential injection points. Successful exploitation could lead to complete database compromise, privilege escalation, and data exfiltration. The combination of findings #3 and #4 suggests systemic SQL injection risks in dotCMS's data access layer. |
| 5 | CVE-2024-40094 | A05:2021 – Security Misconfiguration | GraphQL Java library vulnerability where ExecutableNormalizedFields (ENFs) are not properly considered in DoS protection for introspection queries, allowing attackers to cause denial of service. | True Positive 👀 | Medium | 0.00043 | No | DotCMS implements GraphQL APIs for content delivery and headless CMS operations. The vulnerable graphql-java library is used in the GraphQL module (com.dotcms.graphql package) which exposes content queries, mutations, and schema introspection. An attacker could craft malicious introspection queries that consume excessive CPU and memory resources, effectively causing a denial of service for the GraphQL endpoint. This would impact headless CMS implementations, mobile applications, and SPAs that rely on the GraphQL API for content delivery. In production environments with public-facing GraphQL endpoints, this could disrupt service availability. The vulnerability is particularly concerning for high-traffic sites where even brief DoS conditions could affect thousands of users. Mitigation requires updating to graphql-java 21.5, 20.9, or 19.11. |
| 6 | CVE-2024-47072 | A05:2021 – Security Misconfiguration | XStream vulnerability allowing remote attackers to cause DoS through stack overflow when using BinaryStreamDriver by manipulating the input stream. Fixed in version 1.4.21. | True Positive 👀 | Medium | 0.00044 | No | DotCMS uses XStream for XML serialization/deserialization in various components including bundle processing in the Publishing API (com.dotcms.publisher package). The BinaryStreamDriver may be used when processing remote publishing bundles or importing/exporting content. An attacker with the ability to create or manipulate bundles could cause stack overflow errors, crashing the dotCMS instance or specific worker threads. This is particularly relevant for the push publishing feature where content is synchronized between environments. In multi-node cluster deployments, this could be used to systematically crash nodes. The vulnerability also affects any custom integrations that use XStream for data serialization. While the impact is limited to DoS, it could be leveraged in targeted attacks against specific dotCMS instances during critical operations like content deployment or data migration. |
| 7 | CVE-2025-48734 | A05:2021 – Security Misconfiguration | Improper access control in Apache Commons BeanUtils allowing attackers to access enum class loaders via the "declaredClass" property, potentially leading to arbitrary code execution. | True Positive 👀 | High | 0.00042 | No | DotCMS extensively uses Apache Commons BeanUtils throughout the codebase for JavaBean manipulation, particularly in the content API, form handling, and REST services. The vulnerability allows attackers to access ClassLoaders through enum properties, which could lead to remote code execution. In dotCMS, this could be exploited through several vectors: (1) Content API endpoints that accept property paths from user input, (2) Form submissions where field names are dynamically processed, (3) REST API calls that manipulate content objects using property expressions, and (4) Velocity templates that access bean properties. An attacker could craft malicious property paths targeting enum fields in dotCMS's data models (like ContentletAPI, PermissionAPI objects) to access the ClassLoader and execute arbitrary code. This is particularly dangerous in dotCMS because it supports custom content types with user-defined fields, expanding the attack surface. Successful exploitation could lead to complete server compromise. |
| 8 | CVE-2023-24998 | A05:2021 – Security Misconfiguration | Apache Commons FileUpload DoS vulnerability where unlimited request parts can be processed, allowing attackers to trigger DoS through malicious uploads. Fixed in version 1.5 with FileUploadBase#setFileCountMax configuration. | True Positive 👀 | Medium | 0.08300 | No | DotCMS uses Apache Commons FileUpload for handling multipart file uploads in various areas including: content creation (FileAsset uploads), binary field uploads in custom content types, user profile pictures, and REST API file uploads. The absence of file count limits allows an attacker to submit requests with thousands of parts, consuming server memory and CPU resources. In dotCMS's multi-tenant architecture, this could affect all sites on the instance. The vulnerability could be exploited through the content API (/api/v1/content), WebDAV interface, or direct file upload endpoints. An attacker could automate attacks to continuously submit malicious multipart requests, degrading performance or causing OutOfMemory errors. This is particularly concerning for dotCMS instances that allow anonymous or public uploads (e.g., form submissions with file attachments). The impact extends to clustering scenarios where load balancers might distribute these malicious requests across all nodes. |
| 9 | CVE-2025-48976 | A05:2021 – Security Misconfiguration | DoS vulnerability in Apache Commons FileUpload due to insufficient limits on multipart header allocation, allowing resource exhaustion. Affects versions 1.0-1.5 and 2.0.0-M1 to 2.0.0-M3. | True Positive 👀 | Medium | 0.00042 | No | This vulnerability compounds the risk from CVE-2023-24998 by targeting the header parsing mechanism in FileUpload. DotCMS's file upload functionality in ContentletAPI, FileAssetAPI, and REST services are affected. An attacker can craft multipart requests with excessively large or numerous headers to cause memory exhaustion. In dotCMS, this could be exploited through: (1) Content creation forms with file fields, (2) REST API endpoints accepting file uploads (/api/v1/workflow/actions/default/fire/PUBLISH with file attachments), (3) TempFileAPI operations, and (4) Import/export functionality. The vulnerability is particularly dangerous because it doesn't require authentication in scenarios where public forms or anonymous uploads are enabled. A coordinated attack sending multiple crafted requests could quickly exhaust available memory, causing JVM crashes or forcing garbage collection storms that severely degrade performance across the entire dotCMS instance. |
| 10 | CVE-2024-47554 | A05:2021 – Security Misconfiguration | Uncontrolled resource consumption in Apache Commons IO's XmlStreamReader class allowing excessive CPU consumption when processing maliciously crafted input. Fixed in version 2.14.0. | True Positive 👀 | Medium | 0.00045 | No | DotCMS uses Apache Commons IO extensively for file operations, stream handling, and XML processing. The XmlStreamReader vulnerability affects any component that processes XML content from untrusted sources. In dotCMS, this includes: (1) XML sitemap generation and processing, (2) RSS feed parsing in the Content API, (3) Import/export operations for content and structures, (4) Configuration file parsing, and (5) Integration with external systems via XML APIs. An attacker could upload or submit maliciously crafted XML through content creation interfaces, import functionality, or REST APIs that accept XML payloads. The excessive CPU consumption could lead to thread starvation, making the application unresponsive. In clustered environments, this could cascade across nodes if the malicious XML is replicated or processed by multiple instances. The impact is particularly severe during batch operations like content imports or scheduled jobs that process large XML datasets. |
| 11 | CVE-2024-25638 | A05:2021 – Security Misconfiguration | DNS response validation vulnerability in dnsjava where records in replies aren't checked for query relevance, allowing attackers to inject RRs from different zones. Fixed in version 3.6.0. | True Positive 👀 | Low | 0.00044 | No | DotCMS uses dnsjava for DNS resolution operations, particularly in the EmailAPI for MX record lookups, SPF validation, and potentially in license validation or external service integrations. The vulnerability allows DNS cache poisoning where an attacker could respond with malicious DNS records that appear valid but contain information for different zones. In dotCMS context, this could redirect email sending operations to attacker-controlled mail servers, intercept license validation requests, or redirect API calls to external services. While exploitation requires man-in-the-middle positioning or control over DNS infrastructure, successful attacks could lead to sensitive data exposure (email contents, API keys) or bypass security controls. The impact is lower than other vulnerabilities but still relevant for dotCMS instances that rely on DNS for critical operations like email sending, which is fundamental to workflow notifications, password resets, and user communications. |
| 12 | GHSA-crjg-w57m-rqqf | A05:2021 – Security Misconfiguration | CPU exhaustion vulnerability in dnsjava's ValidatingResolver for DNSSEC validation when processing specially crafted DNSSEC-signed zones (KeyTrap attack). Fixed in version 3.6.0. | True Positive 👀 | Low | N/A | No | If dotCMS is configured to use DNSSEC validation through dnsjava's ValidatingResolver (which is not the default configuration), it becomes vulnerable to the KeyTrap attack. This sophisticated attack exploits DNSSEC validation logic to cause excessive CPU consumption. In dotCMS deployments that implement strict DNS security policies or integrate with DNSSEC-enabled infrastructure, processing malicious DNSSEC responses could lock up the validation threads, preventing DNS resolution for critical operations. This would impact email sending (MX lookups), external API calls, and any feature requiring DNS resolution. However, the likelihood is low because: (1) ValidatingResolver must be explicitly configured, (2) The attacker needs specific DNS infrastructure access, and (3) Most dotCMS deployments use standard non-validating resolvers. The vulnerability is more relevant for high-security government or financial installations that mandate DNSSEC. |
| 13 | GHSA-mmwx-rj87-vfgr | A05:2021 – Security Misconfiguration | Duplicate of GHSA-crjg-w57m-rqqf - CPU exhaustion in dnsjava's ValidatingResolver when processing specially crafted DNSSEC-signed zones. | True Positive 👀 | Low | N/A | No | This is the same KeyTrap vulnerability as GHSA-crjg-w57m-rqqf. The impact on dotCMS is identical: potential CPU exhaustion if ValidatingResolver is explicitly configured for DNSSEC validation. The duplication in the report doesn't increase the risk but confirms the severity of the KeyTrap attack vector. dotCMS administrators using DNSSEC validation should prioritize upgrading dnsjava to version 3.6.0. For most standard dotCMS deployments without explicit DNSSEC configuration, the practical risk remains low. However, the presence of this vulnerability in the dependency tree means that future configuration changes enabling DNSSEC would immediately expose the system to attack without additional warnings. |
| 14 | CVE-2021-37136 | A05:2021 – Security Misconfiguration | Netty Bzip2 decompression decoder vulnerability allowing unlimited decompressed output size, potentially triggering OutOfMemoryError and DoS attacks through malicious compressed input. | True Positive 👀 | Medium | 0.00960 | No | DotCMS uses Netty in its embedded server infrastructure and potentially for async HTTP client operations or WebSocket support. The Bzip2Decoder vulnerability affects any component that decompresses Bzip2-encoded data from untrusted sources. In dotCMS, this could impact: (1) File upload processing if Bzip2 compression is supported, (2) Content import operations accepting compressed archives, (3) Push publishing bundle transfers if compressed, and (4) REST API endpoints accepting compressed payloads. An attacker could craft a small Bzip2-compressed payload that decompresses to gigabytes of data, causing OutOfMemoryError and JVM crashes. This is particularly dangerous in cloud or containerized deployments with memory limits. The vulnerability could be exploited during content migration, bundle installation, or any operation accepting compressed data. While Bzip2 is less common than gzip, any code path that automatically detects and decompresses various formats could be vulnerable. |
| 15 | CVE-2021-37137 | A05:2021 – Security Misconfiguration | Netty Snappy frame decoder vulnerability allowing unlimited chunk length and buffering of reserved skippable chunks, leading to excessive memory usage and potential DoS. | True Positive 👀 | Medium | 0.01000 | No | Similar to CVE-2021-37136, this affects dotCMS's Netty usage but targets Snappy compression. The SnappyFrameDecoder is vulnerable to memory exhaustion through: (1) Chunks with maliciously large declared lengths, and (2) Accumulation of skippable chunks until complete chunk reception. In dotCMS, Snappy compression might be used in: (1) Elasticsearch communication (which uses Netty and may support Snappy), (2) Custom integrations using Snappy compression, (3) Caching layers that compress data, and (4) Websocket communications for real-time features. An attacker providing Snappy-encoded data with huge chunk declarations or numerous skippable chunks could exhaust memory, causing garbage collection storms or OutOfMemory errors. The impact is amplified in high-concurrency scenarios where multiple connections might simultaneously exploit the vulnerability. For dotCMS instances with Elasticsearch or real-time collaboration features, this vulnerability poses a tangible DoS risk. |
| 16 | CVE-2012-6153 | A05:2021 – Security Misconfiguration | Apache HttpClient SSL hostname verification vulnerability allowing man-in-the-middle attacks through improper validation of CN and subjectAltName fields in X.509 certificates. | True Positive 👀 | Low | 0.00970 | No | DotCMS uses Apache HttpClient for making HTTP requests to external services, including: (1) Push publishing to remote instances, (2) REST integrations with third-party APIs, (3) License validation checks, (4) External authentication providers (OAuth, SAML), and (5) Webhook notifications. The vulnerability allows attackers with MITM positioning to present certificates with mismatched hostnames that would be incorrectly accepted. In dotCMS context, this could lead to: (1) Interception of content being pushed to remote instances, exposing potentially sensitive content, (2) Stealing authentication tokens or credentials during OAuth flows, (3) Intercepting license validation requests to bypass licensing, and (4) Capturing webhook payloads containing sensitive event data. However, exploitation requires network-level access (MITM position), making it less likely in properly configured environments. The vulnerability is particularly concerning for dotCMS instances communicating over untrusted networks or through compromised proxies. Organizations should verify they're using HttpClient 4.2.3+ and have proper certificate pinning where critical. |
| 17 | CVE-2021-40690 | A05:2021 – Security Misconfiguration | Apache Santuario XML Security vulnerability where "secureValidation" property isn't properly passed when creating KeyInfo from KeyInfoReference, allowing XPath Transform abuse to extract local XML files. | True Positive 👀 | Medium | 0.00510 | No | DotCMS uses Apache Santuario for XML digital signature processing, likely in: (1) SAML authentication implementations, (2) XML-based API security (WS-Security), (3) Digital signature verification for content integrity, and (4) Integration with enterprise authentication systems. The vulnerability allows an attacker to bypass the secureValidation protection and use XPath Transform in KeyInfoReference elements to read arbitrary local XML files. In dotCMS, this could expose: (1) Configuration files like dotmarketing-config.xml, (2) License files, (3) User database configurations, (4) Integration credentials stored in XML, and (5) Application server configurations. The attack requires the ability to control XML documents being processed for signature verification, which might occur during SAML authentication or when processing signed content bundles. Successful exploitation could lead to sensitive data disclosure and serve as a reconnaissance vector for further attacks. |
| 18 | CVE-2025-55752 | A05:2021 – Security Misconfiguration | Critical path traversal vulnerability in Apache Tomcat's rewrite rules where query parameters rewritten to URL are normalized before decoding, allowing bypass of /WEB-INF/ and /META-INF/ protections, potentially enabling malicious file uploads and RCE. | True Positive 👀 | High | 0.00043 | No | This is a critical vulnerability affecting dotCMS if URL rewriting rules are configured in Tomcat. DotCMS commonly uses Tomcat's rewrite valve for SEO-friendly URLs, redirects, and URL normalization. The vulnerability allows attackers to manipulate request URIs to access protected directories like /WEB-INF/ (containing web.xml, classes, and libraries) and /META-INF/ (containing MANIFEST.MF and other metadata). In dotCMS context, exploitation could lead to: (1) Accessing web.xml to discover servlet mappings and security constraints, (2) Reading configuration files and properties, (3) Accessing compiled classes and analyzing application logic, (4) If PUT is enabled (unlikely but possible in misconfigured instances), uploading malicious JSP files to achieve remote code execution. The vulnerability is particularly dangerous because it affects the web container level, bypassing application-level security controls. DotCMS instances using custom rewrite rules that manipulate query parameters are at highest risk. Immediate upgrade to Tomcat 11.0.11, 10.1.45, or 9.0.109 is critical. |
| 19 | CVE-2016-1000338 | A05:2021 – Security Misconfiguration | Bouncy Castle JCE Provider DSA signature validation vulnerability where ASN.1 encoding isn't fully validated, allowing injection of extra elements while maintaining valid signature. | True Positive 👀 | Low | 0.00155 | No | DotCMS uses Bouncy Castle as a JCE provider for cryptographic operations including: (1) License validation and digital signatures, (2) SSL/TLS operations, (3) Content encryption, (4) JWT token signing/verification, and (5) Password hashing. The vulnerability allows attackers to inject additional ASN.1 elements into DSA signatures that still validate correctly. In dotCMS, this could potentially be exploited to: (1) Add metadata to signed content without invalidating signatures, (2) Manipulate signed messages in ways that bypass integrity checks, (3) Confuse signature verification logic by adding unexpected elements. However, the practical impact is limited because injected data would need to be parsed and acted upon by the application. The likelihood is low because exploitation requires deep understanding of ASN.1 encoding and specific application behaviors. DotCMS should upgrade to Bouncy Castle 1.56+ to eliminate this theoretical attack vector. |
| 20 | CVE-2016-1000340 | A05:2021 – Security Misconfiguration | Bouncy Castle carry propagation bug in raw math classes used for elliptic curve operations, potentially causing spurious calculations in scalar multiplications. | True Positive 👀 | Low | 0.00146 | No | This vulnerability affects Bouncy Castle's low-level elliptic curve implementations used in dotCMS for: (1) ECDSA signature operations, (2) ECDH key agreement, (3) Elliptic curve cryptography in TLS connections, and (4) JWT signing with EC algorithms. The carry propagation bug could cause rare incorrect calculations in elliptic curve scalar multiplications. In dotCMS context, this could theoretically lead to: (1) Invalid signatures being generated that fail verification, (2) Weak or predictable keys in ECDH key exchanges, (3) Authentication failures due to signature mismatches. However, Bouncy Castle's output validation catches these errors with high probability, preventing security breaches. The practical impact is more likely to manifest as intermittent cryptographic failures rather than exploitable vulnerabilities. The bug was fixed in versions after 1.55, and upgrading eliminates the spurious calculation risk. |
| 21 | CVE-2016-1000342 | A05:2021 – Security Misconfiguration | Bouncy Castle ECDSA signature validation vulnerability similar to CVE-2016-1000338 but for ECDSA, where ASN.1 encoding isn't fully validated on signature verification. | True Positive 👀 | Low | 0.00145 | No | This vulnerability affects ECDSA signature validation in dotCMS's Bouncy Castle implementation. ECDSA is widely used in modern systems for: (1) JWT token signing (ES256, ES384, ES512 algorithms), (2) REST API authentication, (3) OAuth/OIDC implementations, (4) Code signing verification, and (5) TLS client certificates. The vulnerability allows extra ASN.1 elements in signature sequences while maintaining validation success. In dotCMS, potential impacts include: (1) Invisible data injection in signed JWT tokens, (2) Manipulation of signed API requests, (3) Bypassing integrity checks on signed content. However, like CVE-2016-1000338, exploitation requires the injected data to be parsed and trusted by the application. The vulnerability is more of a protocol weakness than a direct security breach. DotCMS instances using ECDSA for critical security functions should upgrade to Bouncy Castle 1.56+ to ensure ASN.1 encoding is strictly validated. |
| 22 | CVE-2016-1000343 | A05:2021 – Security Misconfiguration | Bouncy Castle DSA key pair generator vulnerability generating weak private keys when using default values, assuming 1024-bit key size without explicit initialization. | True Positive 👀 | Medium | 0.00145 | No | This vulnerability affects dotCMS if DSA key pairs are generated using Bouncy Castle without explicit parameter initialization. In dotCMS, key generation might occur during: (1) Initial installation and setup, (2) Generating keys for license signing, (3) Creating keys for content encryption, (4) Establishing secure communication channels, and (5) Integration setup requiring cryptographic keys. The vulnerability causes the generator to assume 1024-bit keys and generate weak private values. In cryptographic terms, weak private keys are more susceptible to brute force attacks and cryptanalysis. For dotCMS, this could compromise: (1) Long-term content encryption keys, (2) License validation mechanisms, (3) Inter-cluster communication security, and (4) Integration authentication. The practical impact depends on whether DSA is used (RSA and ECDSA are more common in modern systems). Organizations should audit their key generation code and explicitly initialize key generators with appropriate parameters. Upgrading Bouncy Castle and regenerating any DSA keys created with default parameters is recommended. |
| 23 | CVE-2016-1000344 | A05:2021 – Security Misconfiguration | Bouncy Castle DHIES implementation allowed use of insecure ECB mode, which has been removed from the provider. | True Positive 👀 | Low | 0.00142 | No | DHIES (Diffie-Hellman Integrated Encryption Scheme) in Bouncy Castle could be configured to use ECB (Electronic Codebook) mode, which is cryptographically weak. In dotCMS, if DHIES was used for: (1) Content encryption, (2) Secure data exchange between nodes, (3) File encryption at rest, or (4) Secure storage of sensitive configuration, the ECB mode would reveal patterns in plaintext through identical ciphertext blocks. This is particularly dangerous for structured data like JSON or XML. However, the likelihood of dotCMS specifically using DHIES with ECB is low, as most modern implementations prefer AES-GCM or ChaCha20-Poly1305. The vulnerability has been mitigated in newer Bouncy Castle versions by removing ECB support entirely. DotCMS instances should verify they're not using ECB mode for any encryption operations and upgrade to Bouncy Castle versions that enforce secure mode selection. |
| 24 | CVE-2016-1000352 | A05:2021 – Security Misconfiguration | Bouncy Castle ECIES implementation allowed use of insecure ECB mode, which has been removed from the provider. | True Positive 👀 | Low | 0.00142 | No | Similar to CVE-2016-1000344 but affecting ECIES (Elliptic Curve Integrated Encryption Scheme). ECIES is more commonly used in modern applications than DHIES. In dotCMS, ECIES might be employed for: (1) Encrypting sensitive content fields, (2) Secure key exchange during cluster setup, (3) Protecting credentials in configuration, (4) Mobile app secure communication, and (5) API request encryption. The ECB mode weakness would expose patterns in encrypted data, potentially allowing attackers to infer plaintext structure or content. For example, encrypted credit card numbers or SSNs with ECB mode would show identical ciphertext for identical values, enabling pattern analysis. The vulnerability has been resolved by removing ECB support from ECIES in updated Bouncy Castle versions. DotCMS should audit all encryption implementations to ensure secure modes (GCM, CBC with random IV) are used and upgrade Bouncy Castle to eliminate ECB support entirely. |
| 25 | CVE-2018-1000180 | A05:2021 – Security Misconfiguration | Bouncy Castle RSA key pair generator vulnerability where low-level API generated key pairs with added certainty may have fewer Miller-Rabin tests than expected, potentially creating weak keys. | True Positive 👀 | Medium | 0.00181 | No | This vulnerability affects RSA key generation in Bouncy Castle when using the low-level API with custom certainty parameters. In dotCMS, RSA keys are likely used for: (1) License validation and signing, (2) JWT token signing (RS256, RS384, RS512), (3) SSL/TLS certificates, (4) API authentication, and (5) Content signing and integrity verification. The vulnerability means generated RSA keys might not have the expected primality testing strength, potentially creating composite numbers that are easier to factor. Weak RSA keys compromise all cryptographic operations depending on them. In dotCMS context, this could lead to: (1) License bypass if license validation keys are weak, (2) JWT token forgery if signing keys are compromised, (3) Authentication bypass if API keys are weak, and (4) Content integrity compromise. The impact depends on when keys were generated and what version of Bouncy Castle was used. Organizations should regenerate all RSA keys created with Bouncy Castle versions 1.54-1.59 using BC 1.60+ or BC-FJA 1.0.2+. |
| 26 | CVE-2020-7226 | A05:2021 – Security Misconfiguration | Cryptacular vulnerability allowing attackers to trigger excessive memory allocation during decode operations through manipulated nonce array length in ciphertext headers. | True Positive 👀 | Medium | 0.00127 | No | DotCMS uses Cryptacular (via Apereo CAS integration or directly) for cryptographic operations. Cryptacular's CiphertextHeader.java vulnerability allows untrusted nonce array length values from encoded data to control memory allocation. In dotCMS, this could be exploited through: (1) Encrypted session data, (2) Encrypted content or fields, (3) Secure token decoding, (4) Password reset tokens, and (5) Encrypted configuration values. An attacker providing crafted ciphertext with malicious header values could cause OutOfMemoryError, crashing the JVM or creating garbage collection storms. The vulnerability is particularly dangerous in scenarios where users can provide encrypted data for decoding, such as: password reset flows, encrypted URL parameters, or encrypted cookie values. For dotCMS instances integrated with CAS for SSO, this could affect authentication flows. The attack doesn't require authentication and can be executed remotely. Organizations should upgrade Cryptacular to versions after 1.2.3 and implement input validation for all encoded data accepted from untrusted sources. |
| 27 | CVE-2023-31418 | A05:2021 – Security Misconfiguration | Elasticsearch vulnerability where unauthenticated users can force node exits with OutOfMemoryError by sending malformed HTTP requests to the HTTP layer. | True Positive 👀 | High | 0.00624 | No | DotCMS relies heavily on Elasticsearch for content indexing, search functionality, and analytics. The vulnerability allows unauthenticated attackers to crash Elasticsearch nodes by sending malformed HTTP requests that trigger OOM errors. In dotCMS context, this has severe impacts: (1) Complete search functionality outage, making content discovery impossible, (2) Content API failures for search-based content delivery, (3) Admin UI search and filtering breakdowns, (4) Analytics and reporting disruptions, (5) In clustered Elasticsearch deployments, cascading node failures could bring down the entire cluster. The vulnerability is particularly critical because: (1) No authentication is required, (2) dotCMS cannot function properly without Elasticsearch, (3) Recovery requires restarting affected nodes, causing extended downtime. For high-availability dotC
