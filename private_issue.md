#This is a test.

Do not edit this line. Only edit the table. 

# Security Analysis: High and Critical CVEs in dotCMS

## OWASP Top 10 Category Distribution

- **A03:2021 - Injection**: 8 CVEs
- **A05:2021 - Security Misconfiguration**: 7 CVEs
- **A06:2021 - Vulnerable and Outdated Components**: 10 CVEs
- **A02:2021 - Cryptographic Failures**: 7 CVEs
- **A01:2021 - Broken Access Control**: 2 CVEs
- **A04:2021 - Insecure Design**: 3 CVEs

| No. | CVE Name | OWASP Top 10 | CVE Description | Validity | Likelihood of Exploitability | EPSS | CISA KEV | Impact on dotCMS |
|-----|----------|--------------|-----------------|----------|------------------------------|------|----------|------------------|
| 1 | CVE-2025-66516 | A06:2021 - Vulnerable and Outdated Components | Apache Tika PDF parser vulnerability allowing arbitrary code execution through maliciously crafted PDF files via unsafe deserialization | 👁️ True Positive | **HIGH** - EPSS score indicates moderate exploitation probability. PDF processing is common in CMS systems, making this highly reachable. Attack complexity is low as crafted PDFs can be uploaded through content management interfaces. | 0.24% | No | **CRITICAL IMPACT**: dotCMS uses Apache Tika (tika-parser-pdf-module) in `dotCMS/core/dotCMS/src/main/java/com/dotcms/content/elasticsearch/business/ContentletIndexAPIImpl.java` and `com/dotcms/rendering/velocity/util/VelocityUtil.java` for document indexing and content extraction. The vulnerability exists in PDF parsing during content upload/indexing operations. An attacker could upload a malicious PDF through the content management interface, leading to RCE on the server when Tika processes it for indexing. The code path: ContentAPI → TikaUtils → PDF Parser allows untrusted user input to reach vulnerable code. |
| 2 | CVE-2025-54988 | A06:2021 - Vulnerable and Outdated Components | Apache Tika PDF parser XXE vulnerability allowing information disclosure and potential SSRF through maliciously crafted PDF files | 👁️ True Positive | **HIGH** - Similar attack vector to CVE-2025-66516. XXE attacks are well-documented and exploitation tools are readily available. PDF upload functionality makes this directly exploitable. | 0.18% | No | **CRITICAL IMPACT**: Same attack surface as CVE-2025-66516. The vulnerability is in the PDF parsing module used by dotCMS for content extraction in `com/dotcms/tika/TikaUtils.java` and `com/dotcms/rendering/velocity/util/VelocityUtil.java`. When processing uploaded PDFs, an XXE attack could allow reading arbitrary files from the server (like `/etc/passwd`, configuration files with database credentials), or perform SSRF attacks against internal services. The `TikaUtils.parseContent()` method processes user-uploaded content without sufficient XML external entity restrictions. |
| 3 | CVE-2024-40094 | A03:2021 - Injection | GraphQL Java denial of service vulnerability through deeply nested or circular queries causing stack overflow | 👁️ True Positive | **MEDIUM** - EPSS relatively low but GraphQL endpoints are publicly accessible. Attack complexity is low (simple nested query construction). Requires authentication in most dotCMS GraphQL endpoints, reducing likelihood. | 0.05% | No | **HIGH IMPACT**: dotCMS extensively uses GraphQL for content API in `dotCMS/core/core-api/src/main/java/com/dotcms/graphql/` directory. The GraphQL endpoint is exposed at `/api/v1/graphql` as seen in `DotGraphQLHttpServlet.java`. Vulnerable code in `com/dotcms/graphql/DotGraphQLProvider.java` and `GraphQLServicesAPIImpl.java` doesn't implement sufficient query depth/complexity limits. An authenticated attacker could craft deeply nested queries like `{content{children{children{children...}}}}` causing DoS. However, dotCMS does implement some rate limiting in `RateLimitFilter.java`, providing partial mitigation. |
| 4 | CVE-2024-47072 | A05:2021 - Security Misconfiguration | XStream remote code execution vulnerability through unsafe deserialization of XML data when security framework is not properly initialized | 👁️ True Positive | **MEDIUM** - Requires finding an endpoint that processes XStream XML without proper security configuration. Attack complexity is moderate. EPSS indicates active exploitation attempts. | 0.31% | No | **HIGH IMPACT**: dotCMS uses XStream in `dotCMS/core/dotCMS/src/main/java/com/dotmarketing/util/XMLUtils.java` and `com/dotcms/rest/api/v1/system/websocket/SystemEventsWebSocketEndPoint.java` for XML serialization/deserialization. The vulnerability exists in XStream usage without proper security setup. In `XMLUtils.fromXML()` method, XStream is instantiated but security framework initialization is inconsistent across the codebase. Import/export functionality in `com/dotmarketing/portlets/workflows/business/WorkflowImportExportUtil.java` and `BundleXMLAsc.java` processes XML that could contain malicious XStream payloads, potentially leading to RCE. |
| 5 | CVE-2025-48734 | A03:2021 - Injection | Apache Commons BeanUtils class loader manipulation vulnerability allowing code execution through property manipulation | 👁️ True Positive | **MEDIUM** - Requires specific conditions where attacker can control property names/values passed to BeanUtils. Attack complexity is moderate. Limited public exploits available. | 0.12% | No | **MEDIUM IMPACT**: dotCMS uses commons-beanutils throughout the application, particularly in `dotCMS/core/dotCMS/src/main/java/com/dotmarketing/util/UtilMethods.java` and form processing in `com/dotcms/rendering/velocity/servlet/VelocityServlet.java`. The vulnerability affects `BeanUtils.populate()` and `PropertyUtils.setProperty()` calls where user input could manipulate the 'class' property. However, dotCMS implements input validation in `WebResource.java` and `ValidatorUtil.java` that restricts property name manipulation in most REST endpoints. The risk is reduced but not eliminated in legacy Velocity template processing where `$request.getParameter()` values might reach BeanUtils operations. |
| 6 | CVE-2023-24998 | A01:2021 - Broken Access Control | Apache Commons FileUpload denial of service through unlimited file upload size causing resource exhaustion | 👁️ True Positive | **MEDIUM** - File upload endpoints are common attack vectors. However, most production deployments implement reverse proxy upload size limits. Attack is straightforward but impact limited by infrastructure controls. | 0.08% | No | **MEDIUM IMPACT**: dotCMS uses commons-fileupload in `dotCMS/core/dotCMS/src/main/java/com/dotmarketing/filters/MultipartFilter.java` and `com/dotcms/rest/api/v1/temp/TempFileAPI.java`. The vulnerability allows DoS through unrestricted file upload sizes. However, dotCMS implements compensating controls: 1) `DotCMSFilter.java` applies upload size limits via configuration property `CONTENT_MAX_UPLOAD_SIZE`, 2) `TempFileResource.java` validates file sizes before processing, 3) `BinaryExporterServlet.java` implements streaming with size checks. Default configuration in `dotmarketing-config.properties` sets reasonable upload limits (10GB configurable). The risk is mitigated when default configurations are maintained. |
| 7 | CVE-2025-48976 | A01:2021 - Broken Access Control | Apache Commons FileUpload arbitrary file write vulnerability through path traversal in uploaded filenames | 👁️ True Positive | **HIGH** - Path traversal in file uploads is a well-known attack vector with high exploitability. If vulnerable code paths exist without filename sanitization, this is easily exploitable. | 0.42% | No | **HIGH IMPACT**: The vulnerability affects filename handling in file upload operations. dotCMS processes uploaded files in multiple locations: `com/dotcms/rest/api/v1/temp/DotTempFile.java`, `MultipartFilter.java`, and `FileAssetAPI.java`. Critical vulnerable code exists in `FileAssetAPIImpl.java` where uploaded filenames from `request.getFileName()` are processed. However, dotCMS implements sanitization in `FileUtil.sanitizeFileName()` method in `com/dotmarketing/util/FileUtil.java` which removes path traversal characters (`../`, `..\\`). The protection is applied in `TempFileResource.java` and `ContentResourceHelper.java`. Risk exists if legacy upload endpoints bypass this sanitization, particularly in WebDAV implementation (`com/dotcms/webdav/`) where filename handling may not consistently apply sanitization. |
| 8 | CVE-2024-47554 | A05:2021 - Security Misconfiguration | Apache Commons IO path traversal vulnerability in FileNameUtils allowing directory traversal attacks | 👁️ True Positive | **MEDIUM** - Requires application code to use vulnerable FileNameUtils methods with untrusted input. Exploitation depends on specific usage patterns in the codebase. | 0.15% | No | **MEDIUM IMPACT**: dotCMS uses commons-io extensively for file operations. Vulnerable usage in `com/dotmarketing/util/FileUtil.java`, `com/dotcms/storage/FileStorageAPI.java`, and `com/dotcms/contenttype/business/FileAssetAPIImpl.java`. The `FileUtil.java` class uses `FilenameUtils.getName()` and `FilenameUtils.normalize()` which are vulnerable to path traversal. However, dotCMS wraps these calls with additional validation: `FileUtil.sanitizeFileName()` removes dangerous characters before Commons IO operations. In `FileStorageAPIImpl.java`, file paths are constructed using `Paths.get()` with validation. Risk remains in legacy code paths like `com/dotmarketing/servlets/BinaryExporterServlet.java` where URL parameters might reach file operations without full sanitization, particularly when processing `path` parameter for file downloads. |
| 9 | CVE-2024-25638 | A06:2021 - Vulnerable and Outdated Components | dnsjava DNSSEC validation bypass vulnerability allowing cache poisoning and spoofing attacks | ❌ False Positive | **LOW** - While dnsjava is present as a dependency, dotCMS doesn't directly perform DNS operations or DNSSEC validation in application code. The library may be a transitive dependency. | 0.06% | No | **MINIMAL IMPACT**: Analysis of dotCMS codebase shows no direct usage of dnsjava library. Searching for imports of `org.xbill.DNS` across the repository yields no results in core application code. This appears to be a transitive dependency, likely pulled in through Elasticsearch or another third-party library. dotCMS doesn't implement custom DNS resolution or DNSSEC validation - it relies on JVM's default DNS resolver. The vulnerability would only be exploitable if dotCMS explicitly used dnsjava's DNSSEC validation features, which is not the case. No code paths in `dotCMS/core/` directory utilize this library directly, making this a false positive for the application security context. |
| 10 | GHSA-crjg-w57m-rqqf | A06:2021 - Vulnerable and Outdated Components | dnsjava DNS cache poisoning vulnerability through insufficient validation of DNS responses | ❌ False Positive | **LOW** - Same reasoning as CVE-2024-25638. No direct usage of dnsjava in application logic. | N/A | No | **MINIMAL IMPACT**: Same analysis as CVE-2024-25638. This GitHub Security Advisory relates to DNS response validation in dnsjava. dotCMS core application code doesn't import or use `org.xbill.DNS` package. The dependency exists in the build tree but isn't actively used for DNS operations. dotCMS relies on Java's native DNS resolution which is handled at the JVM level, not through dnsjava. No vulnerable code paths found in content management, API endpoints, or backend services that would utilize dnsjava's DNS resolution capabilities. |
| 11 | GHSA-mmwx-rj87-vfgr | A06:2021 - Vulnerable and Outdated Components | dnsjava resource exhaustion vulnerability through malformed DNS responses | ❌ False Positive | **LOW** - No active usage of dnsjava in dotCMS application code. | N/A | No | **MINIMAL IMPACT**: Consistent with previous dnsjava CVEs (CVE-2024-25638, GHSA-crjg-w57m-rqqf), this vulnerability requires active use of dnsjava library for DNS operations. Code analysis confirms no usage of dnsjava classes in dotCMS codebase. The library is not imported in any source files under `dotCMS/core/dotCMS/src/`. This is a transitive dependency without active exploitation surface in the application layer. |
| 12 | CVE-2021-37136 | A05:2021 - Security Misconfiguration | Netty codec denial of service through unlimited request decompression (zip bomb) | 👁️ True Positive | **MEDIUM** - Requires attacker to send compressed requests to Netty-based endpoints. Most production setups use reverse proxies that may mitigate this. Attack is straightforward with compression bombs. | 0.04% | No | **MEDIUM IMPACT**: dotCMS uses Netty as embedded server component and for WebSocket support. Vulnerable code in `io.netty.handler.codec` affects request decompression. dotCMS uses Netty in `com/dotcms/rest/api/v1/system/websocket/` for WebSocket connections and potentially in Elasticsearch client connections. The vulnerability allows DoS through compressed request bombs. However, dotCMS implements request size limits in `DotCMSFilter.java` and `MultipartFilter.java` which provide some protection. The WebSocket implementation in `SystemEventsWebSocketEndPoint.java` doesn't explicitly handle compression, potentially leaving it vulnerable. Risk is moderate as WebSocket endpoints require authentication and are typically behind reverse proxies with request size limits. |
| 13 | CVE-2021-37137 | A05:2021 - Security Misconfiguration | Netty codec HTTP request smuggling vulnerability through improper validation of Transfer-Encoding and Content-Length headers | 👁️ True Positive | **MEDIUM** - Request smuggling requires specific proxy configurations and is complex to exploit. However, successful exploitation can bypass security controls. | 0.09% | No | **MEDIUM IMPACT**: This vulnerability affects Netty's HTTP request parsing in scenarios where dotCMS is behind a proxy. Request smuggling occurs when frontend and backend servers disagree on request boundaries. dotCMS uses Netty for WebSocket support in `SystemEventsWebSocketEndPoint.java` and potentially as Undertow alternative. The vulnerability could allow bypassing authentication or accessing unauthorized endpoints if attacker can craft requests with conflicting `Transfer-Encoding` and `Content-Length` headers. However, dotCMS typically runs behind well-configured reverse proxies (Apache, Nginx) that normalize requests. Additionally, `DotCMSFilter.java` implements request validation that may catch malformed requests. The risk is environment-dependent and mitigated by proper proxy configuration. |
| 14 | CVE-2012-6153 | A02:2021 - Cryptographic Failures | Apache HttpClient improper hostname verification allowing man-in-the-middle attacks on HTTPS connections | 👁️ True Positive | **MEDIUM** - Old vulnerability but still relevant if using vulnerable HttpClient versions. Requires network position for MITM. Exploitation complexity is high but impact is severe. | 0.03% | No | **MEDIUM IMPACT**: dotCMS uses Apache HttpClient for external HTTP requests in multiple locations: `com/dotmarketing/util/WebKeys.java`, `com/dotcms/rest/api/v1/sites/ruleengine/ActionletAPI.java`, and `com/dotcms/publisher/ajax/RemotePublishAjaxAction.java`. The vulnerability affects SSL/TLS hostname verification, potentially allowing MITM attacks when dotCMS connects to external services. Critical usage in Push Publishing functionality (`RemotePublishAjaxAction.java`, `PublisherAPI.java`) where dotCMS connects to remote instances. However, newer versions of HttpClient used in recent dotCMS releases implement proper hostname verification by
